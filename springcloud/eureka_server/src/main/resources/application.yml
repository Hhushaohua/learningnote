server:
  port: 8040
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://127.0.0.1:8040/eureka/
  server:
spring:
  application:
    name: eureka